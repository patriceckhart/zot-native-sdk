# @zot-native/core

Shared provider options, event types, and session interfaces for the zot native SDK packages. Most applications install a platform package instead of importing this package directly.
